# 開発ルール（Claude Code / Codex 共通）

**このファイルが開発ルールの唯一の正です。**
`CLAUDE.md` はこのファイルを指すポインタで、運用手順（ガチャ更新など）だけを持ちます。
ルールの追加・変更は必ずこのファイルに対して行うこと。両方に書くと必ずズレます。

---

## 0. 作業開始時に必ずやること

1. このファイルを最後まで読む
2. 下の索引から、今回の作業に該当する `docs/` を読む
3. 作業ブランチを切る。**ブランチ名は進捗管理チャットが提示する**（`docs/branch-naming.md`）
4. **着手前に「変更する予定のファイル一覧」を宣言する**

---

## 1. 索引 — 場面ごとに読むもの

| 作業内容 | 読むファイル |
|---|---|
| モンスターID・採番・URL・画像名 | `docs/monster-id.md` |
| ディレクトリ構造・URL設計 | `docs/directory.md` |
| ブランチを切るとき | `docs/branch-naming.md` |
| **本番に影響するか判断するとき** | **`docs/production-impact.md`** |
| 休止中ファイル（読むな・消すな） | `docs/dormant-files.md` |
| ビルド処理 | `docs/build-spec.md` |
| 全体計画・各段階の完了条件 | `docs/ライ徹_開発計画.md` |
| 現在地・次の作業・引き継ぎ | `docs/PROGRESS.md` |
| 管理者がClaudeへ依頼して開発・公開するとき | `docs/admin-development.md` |
| 次回のClaude作業を開始するとき | `docs/claude-next-session.md` |
| アシストカードDB・静的ページ・CMSの設計と進捗 | `docs/assist-card-cms-progress.md` |
| コンテンツ運用（ガチャ更新・日記追加） | `CLAUDE.md` |

**索引にない作業を始める前に、関連する `docs/` が無いか確認すること。**

---

## 2. 読み込み禁止

`docs/dormant-files.md` に列挙されたファイルとディレクトリを読み込まないこと。

広告審査のため一時的に取り下げたページとそのデータで、**合計1.2MB以上**あります。
現在の作業には不要で、1本読むだけで作業1回分のトークンが消えます。

### 大きいファイルの扱い

**100KB を超えるファイルは、内容を読まずにスクリプトで処理すること。**

```
×  「assist-card-data.js を読んでカード名を一覧化して」
○  「assist-card-data.js からカード名を抽出する Node スクリプトを書いて実行して」
```

### 画像

画像ディレクトリを一括で読み込まないこと。必要な数枚だけを個別に開くこと。

---

## 3. 変更禁止

| 対象 | 理由 |
|---|---|
| **既存の公開HTMLの移動・リネーム** | URL変更は被リンクとインデックスを失う。GitHub Pagesはリダイレクトを設定できない |
| **`monsters-data.js` の配列順** | Firestoreの解説データが配列インデックスをキーにしている。過去2回事故 |
| **確定済みのモンスターID** | `src/data/sheet-sortorder.json` の値は不変。振り直し禁止 |
| **`docs/dormant-files.md` 記載のファイルの削除** | 審査通過後に復活させる |
| **画像ディレクトリの統合・リネーム** | 全HTMLの `<img src>` 書き換えが発生し、得るものが無い |
| **`google59378bd79752d094.html` の削除・移動** | Search Console の所有権確認ファイル。消すと計測権限を失う。ルート直下から動かさない |

対象の公開HTML: `index.html` `beginner.html` `newbie.html` `tips.html` `reroll.html`
`privacy.html` `profile.html` `contact.html` `diary.html` `monsters.html` `assist.html`
`motonoyatu.html` `ikusei/*.html` `diary/*.html`

**新規に生成するページは対象外**です。自由に設計してください。

---

## 4. 順序の制約（違反すると復旧不能）

> **`monsters-data.js` の配列順を変更する前に、必ず以下を完了すること。**
>
> 1. `node generate-ids.js` — `src/data/monster-ids.json` に `arrayIndex` を記録
> 2. Firestore のエクスポート — `arrayIndex` を使ってID変換

Firestoreの解説データは `docId = String(配列インデックス)` で保存されています。
順序が変わると **65,658字の運営者解説が全部別のモンスターに紐づきます。**

---

## 5. 検証

**`node scripts/verify.js` が通らない変更はコミットしないこと。**

ルールのうち機械的に判定できるものはこのスクリプトに実装されています。
文章で書かれたルールより優先されます。新しくルールを作るときは、
**まず「これはverify.jsで検査できないか」を考えること。**

---

## 6. 作業の原則

- **指示されたファイル以外を変更しない。**「ついでに整理」をしない
- 既存の `style.css` のクラス名を使い、新しいCSSを追加しない
- 「使われていなさそう」という自己判断でファイルを削除しない
- 自動生成ファイルを手で編集しない。必要な生成物は `build.js` またはCMS公開Workflowに生成させる
- 秘密情報をコードに書かない。P11-6で公開クライアントの平文パスワードは0件になった。
  `scripts/verify.js`が再混入をFAILにする

### CMS管理対象（モンスター）

- モンスターの新規登録・属性・解説・編成・画像は**GAS管理画面から変更する**
- GASの開発・監査用ソースは`_cms/gas/コード.gs`と`_cms/gas/index.html`を正とする。
  先頭が`_`のディレクトリはGitHub PagesのJekyll公開対象に含めず、`include`にも追加しない。
  `_cms`を置く間はJekyll処理を無効にする`.nojekyll`も追加しない
- `_cms/gas`は稼働中GASから読み取りexportした基準である。通常作業で直接編集せず、
  GASへの同期・保存・deployは管理者が別タスクで明示承認した場合だけ行う
- GASの秘密値はScript Propertiesだけに置き、`_cms/gas`へ書かない
- 現在の`コード.gs`先頭コメントには旧予定パス`cms/gas/コード.gs`が残る。
  完全一致の基準を保つため、別承認のGAS同期タスクまで変更しない
- `monsters-data.js`、`src/data/monsters-editorial.json`、
  `src/data/cms-id-predictions.json` をリポジトリ側で直接編集しない。次のCMS公開で上書きされる
- `monster/<4桁ID>.<拡張子>` はDrive経由で管理する。リポジトリ側で差し替えない
- `cms/publish` はGAS専用ブランチ。手動push・マージ・通常作業への流用は禁止
- `cms/protected-test`と`p11-7/*`はP11-7の保護経路test専用で、Pages配信・本番CMS公開に使わない。
  main向けに変更せず、test完了後は管理者手順に従って削除する
- 公開は「GAS → `cms/publish` → ID生成・ID検算・build・verify → `main`」の順。
  途中で失敗した場合は `main` を更新しない
- CMS公開Workflowが`main`へ直接pushする現状では、CMS用の迂回設定または同等の公開経路を
  実機で確認するまで`main`のブランチ保護を有効化しない
- カード、日記、トップページなどCMS対象外の作業は従来どおり作業ブランチで行う。
  **着手前に最新の `main` を取り込むこと**。CMS公開は対象外ファイルを保持する
- 管理者向けの環境確認、Claudeへの依頼、push・PR・マージの手順は
  `docs/admin-development.md`を正とする
- GitHub Pagesは現在 `main` を直接配信している。通常PRのマージもCMS公開も、`main` 更新＝本番公開

---

## 7. 完了報告の形式

作業完了時に、必ず以下を報告すること。

```
変更したファイル:
  - path/to/file  （何をしたか）
変更していないことを確認したファイル:
  - （指示に含まれていたが触らなかったもの、およびその理由）
verify.js:  PASS / FAIL（FAILの場合は内容）
未完了・要判断:
  - （あれば）
```

---

## 8. 迷ったら止まる

以下に該当したら、自己判断で進めずに報告して指示を仰ぐこと。

- ルールどうしが矛盾している
- 指示されたことをするには変更禁止の対象に触る必要がある
- データに想定外の不整合がある（重複ID、血統の欠落など）
- 同じ手法で2回失敗した

**勝手に回避策を実装しないこと。** 止まって報告するほうが安全です。

---

## 9. 環境

- 所有者の環境は Windows。パス区切りとファイル名の大小文字に注意
- `.gitattributes` で `text=auto`。改行コードの一括変換を発生させないこと
- Node.js で動くスクリプトを基本とする（追加パッケージは事前に相談）
- Python を使う場合は `pillow` `requests` のみ前提とする

---

## 10. このルールの育て方

- **開発環境に関わるルール** → このファイルに追記
- **ドメイン知識・データの規則**（ID採番、血統、ゲーム仕様など） → `docs/` に新規ファイルを作り、
  第1章の索引に1行追加する
- このファイルは**短く保つ**こと。長くなると読み飛ばされる。目安は200行以内
