# LINEモンスターファーム徹底攻略 - コンテンツ運用メモ

> **開発ルールは `AGENTS.md` が唯一の正です。作業前に必ず読んでください。**
> このファイルはガチャ更新・モンスター更新・日記追加など、コンテンツ運用だけを扱います。

**最終更新：2026-09-01（旧URLの恒久インデックス削除 sitemap-legacy.xml を追加）**

Git・ブランチ・PR・マージの管理者向け手順は`docs/admin-development.md`を参照してください。

---

## 🔴 最初に確認：モンスターはCMS管理

モンスターの新規登録・属性・解説・編成・画像は、GAS管理画面から変更します。
リポジトリの次のファイルを直接編集してはいけません。

- `monsters-data.js`
- `src/data/monsters-editorial.json`
- `src/data/cms-id-predictions.json`
- `monster/<4桁ID>.<拡張子>`

直接編集しても次のCMS公開で上書きされます。既存モンスター名はIDと公開URLの根拠なので、
リポジトリ・シートのどちらからも変更しません。

公開フローは固定です。

```text
GAS管理画面で保存・画像アップロード
  → adminが「公開」
  → GASがcms/publishへ1コミット
  → generate-ids.js
  → シート予測IDと採番結果を照合
  → build.js
  → scripts/verify.js
  → 全成功時だけmainへ反映・公開
```

失敗時は`main`と公開サイトを更新しません。管理画面の「公開結果を確認」と
`publish_log`で、失敗したActionsステップを確認します。

### 現在の実数

| 項目 | 値 |
|---|---:|
| モンスター総数 | 351 |
| インデックス対象 | 52 |
| noindex | 299 |
| sitemap | 82URL（既存24 + モンスター52 + モン類6） |

インデックス判定は生成HTMLの可視文字数800字以上です。UI表示は予測で、最終判定は
`build.js`が行います。sitemap合計は`existingBlocks + pages`で動的計算されるため、
モンスターの昇格時に定数を手で直す必要はありません。

---

## 🟢 モンスターの更新手順

### 既存モンスターを編集する

1. GAS管理画面を開く
2. 一覧から対象を選ぶ
3. 解説・編成・著者情報などを編集して保存
4. 画像を変える場合は同じ画面からアップロード
5. adminが「公開」
6. 上部表示が「公開成功」になるまで確認

名前は管理画面から変更できません。血統変更はIDへ影響しうるため、確認ダイアログの内容を
必ず読みます。同時編集エラーが出たら上書きせず、画面を再読み込みしてください。

### 新規モンスターを登録する

1. GAS管理画面の「＋ 新規モンスター」
2. 名前・オーラ・モン類・主血統・副血統を入力
3. 画面に出る予測IDを確認して登録
4. 登録後の編集画面から画像をアップロード
5. 必要なら解説・編成・著者情報を保存
6. adminが「公開」
7. Actionsの`Verify CMS predicted IDs`がPASSしたことを確認

採番の正は`generate-ids.js`です。GASの値は予測であり、C8が全件を最終検算します。
ID不一致が出た場合、シートA列や`BLOOD_ORDER`を自己判断で直さず、エラー全文を共有します。

### 公開後にトップのピックアップも変える場合

トップページはCMS対象外です。モンスター公開が成功して新URLが確定してから行います。

1. リモートの最新`main`を取り込む
2. 通常の作業ブランチを作る
3. `index.html`の画像・説明・リンクを新しい4桁IDのURLへ更新
4. 更新履歴の一番上へ追記
5. `node scripts/verify.js`がFAIL 0であることを確認
6. PRを`main`へマージ

旧形式の`monsters/monster.html?id=<配列インデックス>`へ新しくリンクしません。

---

## 🟢 ガチャ更新テンプレート（コピペして埋めて貼る）

```text
【ガチャ更新】
モンスター1: {名前} / {オーラ色} / {モン類} / {メイン種族} / {サブ種族} / {画像ファイル名}
モンスター2: {名前} / {オーラ色} / {モン類} / {メイン種族} / {サブ種族} / {画像ファイル名}
カード1: {レアリティ} / {名前} / {オーラ色} / {モン類} / {タイプ} / event2:{イベント2} / {画像ファイル名}
カード2: {レアリティ} / {名前} / {オーラ色} / {モン類} / {タイプ} / event2:{イベント2} / {画像ファイル名}
能力画像: {ファイル名1}, {ファイル名2}, {ファイル名3}, {ファイル名4}
ピックアップ文モンスター1: |
  {1行目}
  {2行目}
ピックアップ文モンスター2: |
  {1行目}
  {2行目}
ピックアップ文カード1: |
  {1行目}
  {2行目}
ピックアップ文カード2: |
  {1行目}
  {2行目}
```

「テンプレ出して」と言われたら、上のコードブロックをそのまま出します。

モンスター部分はGAS管理画面、カードとトップページ部分はリポジトリ作業です。
一度に更新する場合も、先にCMSでモンスター公開を成功させ、最新`main`を取り込んでから
カード・トップページ用の作業ブランチを作ります。

---

## 🟢 Geminiへのプロンプト（ガチャ更新用）

Geminiに以下を貼り付けてYouTube URLを添えて送ります。出力の「？」のうちオーラ色だけ
埋めて渡せばOKです。`event2`は後で個別更新でも構いません。

```text
以下のYouTube動画（LINEモンスターファームのガチャ解説動画）を見て、
下記フォーマットで情報を出力してください。

[動画URL]

■出力フォーマット（そのまま埋めてください）：

【ガチャ更新】
モンスター1: {名前} / {オーラ色} / {モン類} / {メイン種族} / {サブ種族} / ？
モンスター2: {名前} / {オーラ色} / {モン類} / {メイン種族} / {サブ種族} / ？
カード1: {レアリティ} / {名前} / {オーラ色} / {モン類} / {タイプ} / event2:？ / ？
カード2: {レアリティ} / {名前} / {オーラ色} / {モン類} / {タイプ} / event2:？ / ？
ピックアップ文モンスター1: |
  （動画の解説文を箇条書きで、1行20文字以内で改行）
ピックアップ文モンスター2: |
  （同上）
ピックアップ文カード1: |
  （同上）
ピックアップ文カード2: |
  （同上）

■注意事項：
- オーラ色：黒・赤・青・白・緑・黄 のいずれか
- モン類：獣族・無機・魔族・創造・幻霊・怪物 のいずれか
- 種族（メイン・サブ共通）：アーク・アローヘッド・イルミネ・ウンディーネ・ガリ・カワズモー・キジン・キュービ・グジラ・ゲル・ケンタウロス・ゴースト・ゴーレム・ザン・ジョーカー・シンリュウ・スエゾー・ディノ・デュラハン・ドラゴン・ナーガ・ニャー・ネンドロ・ノーブル・ハム・ピクシー・ヒノトリ・プラント・ヘンガー・メタルナー・モッチー・モノリス・ユグドラシル・ライガー・レアモン・ロード種・ワーム のいずれか（サブ種族が不明な場合はレアモン）
- レアリティ：MR または SSR
- カードタイプ：アキュメン・アサルト・インパクト・ガード・かしこさ・クイック・サバイブ・ジャッジ・タフネス・ちから・テクニック・バイタル・フィジカル・フォーカス・フォース・メンタル・ライバル・ライフ・ルミナス・回避・師匠・丈夫さ・命中・友人 のいずれか
- ？の箇所は動画から判断できないので「？」のまま残してください
- モンスターが1体・カードが1枚の場合は該当行を削除してください
```

`event2`が不明なら`event2:調査中`とします。能力画像が無い場合は該当行を省略できます。

---

## ⚠️ 新アシストカード追加時の注意

カードはCMS対象外です。リモートの最新`main`を取り込んで通常の作業ブランチで更新します。

### セットで更新するファイル

1. `cards/cards-data.js` → 先頭に追加
2. `assist.html` → `#cardGrid`の先頭にカードHTMLを追加
3. `index.html` → ピックアップ画像・リンク・説明文を更新
4. `reroll.html` → 「現在のおすすめ」と更新日を更新
5. `assist-cards/` → カード画像を配置
6. `assist-abilities/` → 能力画像を配置
7. 下の「現在のピックアップ状態」を更新

`assist-abilities/`への画像配置はできますが、カードとの対応はFirestoreの
`cardAbilities/assignments`に保存されています。`ability-match.html`は現在書き込めないため、
新規割り当てが必要になったら止まって相談してください。Firestoreルールは緩めません。

```html
<a class="card" data-rarity="MR" href="cards/card.html#カードID">
  <img class="card-img" src="assist-cards/カードID.jpg" alt="カード名">
  <div class="card-info"><div class="card-name">カード名</div><span class="rarity rarity-MR">MR</span></div>
</a>
```

モンスター編成で新カードを使う場合は、先に`cards/cards-data.js`へカードIDを登録します。
未登録IDは`build.js`の警告に出るため0件にしてください。

### カードIDは自分で考えない

CMSの「＋ 新規カード」で登録すると、cardIdはサーバーが`c<4桁連番>-<レアリティ>`
（例 `c0092-MR`）で自動採番します。名前・レアリティ・オーラ・タイプ・モン類だけを入力し、
IDは入力しません。画像は採番されたIDをファイル名にしてアップロードします。
既存91件の旧形式ID（`a13i-MR-nishiki`など）は公開URLの実体なので変更しません。
詳細は`docs/assist-card-create-runbook.md`の第3章です。

---

## ⚠️ 技（スキル）DBの追加・更新

技は血統ごとに共通で、血統ページ `monsters/<モン類>/<血統>/index.html` に表示します。
モンスター詳細ページに全量を出すと重複コンテンツになるため、置き場所は血統ページだけです。

現在はまだCMS対象外で、リポジトリのJSONを直接編集します（CMS化はS3）。

1. `src/data/skill-abilities.json` に技能力を追加（`sab-####`）
   - `description` の改行は `\n`。HTMLタグは書かない
2. `src/data/monster-skills.json` に技を追加（`sk-####`）
   - `range` は技を**発動できる**間合い。`moveTo` は発動後に移動する技だけ書き、
     移動しない技は `null`（`A→B` の A は `range` と必ず一致するので持たない）
   - 評価（ダメージ・命中率・ガッツダウン・クリティカル率）は `S+`〜`G`。各段に `+` 付きがある
   - 固有技は `unique: true` にして `owners` へ使えるモンスターの4桁IDを配列で入れる
   - `unlockedBy` は技登録の解放元。`{ "monsterId": "2626", "rarity": 4 }` の配列で、
     `rarity` は登録に必要な★の数（1〜10）。ページには「セイラン（★4以上）」と出る
3. `node build.js` → 技が1件以上ある血統だけページが生成される
4. `node scripts/test-skill-build.js` と `node scripts/verify.js` がFAIL 0

技が0件の血統はページを生成しません。**分かる血統から1つずつ入れられます。**
可視800字未満のうちは `noindex,follow` になり、技が増えれば自動でインデックス対象へ昇格します。

詳細な設計は `docs/skill-design.md`、ページ構成は `docs/build-spec.md` の 3-2 と 5-9 を参照。

---

## ⚠️ プレイ日記追加時の注意

日記は1記事＝1ページです。`diary.html`へ本文全文を追記しません。

1. `diary/`に英字スラッグの新規HTMLを作る
   - title・description・canonical・h1を記事ごとにユニークにする
   - `diary-nav`を更新し、直前記事の「次の記事」も新記事へ向ける
2. `diary.html`の先頭へ日付・タグ・タイトル・抜粋カードを追加
3. インデックス対象なら`sitemap.xml`の既存URL部分へ新URLを追加
4. インデックス対象を増やした場合だけ、`build.js`の
   `existingBlocks.length !== 24`を新しい既存URL数へ更新
   - sitemap合計は動的計算なので別の合計定数は直さない
5. `index.html`の日記プレビューを最新4件へ更新
6. `index.html`の更新履歴へ追記
7. `node build.js`と`node scripts/verify.js`を実行

800字未満の短い記事は`noindex,follow`を付け、sitemapへ載せません。この場合、手順4の
既存URL数も変えません。記事が育ったらnoindexを外し、sitemapへ追加できます。

---

## ℹ️ 画像とファイルの管理区分

### CMS管理（リポジトリから編集しない）

| 対象 | 管理方法 |
|---|---|
| モンスター属性 | GAS → `monsters-data.js` |
| 解説・編成・著者 | GAS → `src/data/monsters-editorial.json` |
| 予測ID一覧 | GAS → `src/data/cms-id-predictions.json` |
| 新規・差し替え画像 | GAS → Drive `ライ徹_画像/monster/` → `monster/<4桁ID>.<拡張子>` |

画像ファイル名は管理画面が`<4桁ID>.<拡張子>`に統一します。シートにはファイル名だけを保存し、
`monster/`は付けません。解決順は`build.js`の`resolveImage()`が正です。

### build.js生成（手で編集しない）

- `src/data/monster-ids.json`
- `src/data/id-availability.json`
- `src/data/page-baseline.json`
- `src/data/cms-seed.json`
- `monsters/**/*.html`（血統ページ `monsters/<モン類>/<血統>/index.html` を含む）
- `monsters/redirect-map.js`
- `monsters.html`の`BUILD:MONSTER-CARDS`マーカー内
- sitemapのモンスター・モン類URL部分

### リポジトリで管理

- `src/data/monster-skills.json`、`src/data/skill-abilities.json`（技DB。現在は手入力）
- `cards/cards-data.js`、`assist.html`、`reroll.html`
- `index.html`のピックアップ・日記プレビュー・更新履歴
- `diary.html`、`diary/*.html`
- `assist-cards/`、`assist-abilities/`
- その他の手書き記事

---

## ℹ️ Firestoreとコメント

- Firestoreは読み取り許可・書き込み禁止です。ルールを緩めません
- 既存コメント76件・35モンスターは残っていますが、現在サイトには表示していません
- 新規コメント投稿は停止中です
- `monster-match.html`、`ability-match.html`は書き込みを伴うため使用しません
- コメント表示・投稿の再開はAdSense通過後の別タスクです

---

## ℹ️ 旧URLの恒久インデックス削除（一時運用中）

旧URL `monsters/monster.html?id=N` をGoogleのインデックスから消すため、旧URLだけを
載せた `sitemap-legacy.xml` をGSCへ手動送信しています。**一時的な仕組みです。**

- 生成は `node scripts/gen-legacy-sitemap.js`（手動実行のみ。`build.js` からは呼ばない）
- `sitemap.xml` に混ぜない・`robots.txt` に `Sitemap:` 行を追加しない
- 撤去の判断基準・期限・手順は`docs/legacy-url-sitemap-runbook.md`

---

## 🔴 リポジトリ作業の完了条件

- 作業開始時に最新`main`を取り込む（CMS公開で`main`が自動更新されるため）
- 生成入力へ影響する変更では`node build.js`を実行する
- すべての変更で`node scripts/verify.js`を実行し、FAIL 0を確認する
- 公開コンテンツを更新した場合は`index.html`の更新履歴へ追記する
- 全ファイルの更新完了前に「完了」と報告しない
- GitHub Pagesは`main`を直接配信するため、PRマージ＝本番公開

CMSだけの編集ではリポジトリを手作業せず、管理画面の「公開成功」を完了条件にします。

---

## 🔴 現在のピックアップ状態

| 項目 | 値 |
|---|---|
| ピックアップモンスター | カッパマキ |
| モンスターID | `1625` |
| index.htmlのリンク | `monsters/genrei/mocchi/1625.html` |
| monsters-data.js末尾インデックス | 350（351体・0始まり） |
| ピックアップカード | ジンゴロウ（MR） |
| カードID | `g3-MR-jingorou` |
| index.htmlのリンク | `cards/card.html#g3-MR-jingorou` |
